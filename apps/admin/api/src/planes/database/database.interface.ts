/**
 * DatabaseService — re-exported from transport-types.
 * Admin API uses the base interface (no LangGraph checkpoint support needed).
 */
export {
  DATABASE_SERVICE,
  type DatabaseService,
  type QueryResult,
  type QueryBuilder,
} from '@orchestrator-ai/transport-types';
