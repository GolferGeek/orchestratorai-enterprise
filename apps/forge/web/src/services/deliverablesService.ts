/**
 * Deliverables service - barrel file.
 * TYPES ONLY. The impl is NOT re-exported here to prevent circular dependency TDZ errors.
 * For the service instance, import getDeliverablesService directly from deliverablesService.impl.
 */
export * from "./deliverablesService.types";
