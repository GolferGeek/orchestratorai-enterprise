# =============================================================================
# ORCHESTRATOR AI API - ENVIRONMENT CONFIGURATION
# =============================================================================
# This file contains API-specific environment variables
# Copy this to .env and replace placeholders with your actual values
# =============================================================================

# =============================================================================
# VISION MODEL CONFIGURATION (for multimodal document processing)
# =============================================================================
# Used by Legal Department AI for extracting text from images and scanned documents
# Supported providers: openai, anthropic, ollama, google
#
# Recommended models:
#   OpenAI: gpt-4-vision-preview, gpt-4o
#   Anthropic: claude-3-opus-20240229, claude-3-sonnet-20240229
#   Ollama: llava:13b, llava:34b (requires local installation)
#   Google: gemini-pro-vision
VISION_MODEL=gpt-4-vision-preview
VISION_PROVIDER=openai

# Ollama base URL (only needed if using Ollama for vision)
OLLAMA_BASE_URL=http://localhost:11434

# =============================================================================
# STORAGE BUCKETS CONFIGURATION
# =============================================================================
# Supabase storage bucket names for different types of content
# These buckets must be created in Supabase Storage
MEDIA_STORAGE_BUCKET=media
LEGAL_DOCUMENTS_BUCKET=legal-documents

# =============================================================================
# NOTES
# =============================================================================
# 1. Vision models require API keys for the respective providers:
#    - OpenAI: Set OPENAI_API_KEY in main .env
#    - Anthropic: Set ANTHROPIC_API_KEY in main .env
#    - Google: Set GOOGLE_API_KEY in main .env
#    - Ollama: No API key needed (runs locally)
#
# 2. Storage buckets must be created manually in Supabase Storage:
#    - Navigate to Supabase Dashboard > Storage
#    - Create bucket: media
#    - Create bucket: legal-documents
#    - Set appropriate policies (RLS) for file access
#
# 3. Vision extraction is primary method, OCR is fallback:
#    - Vision models provide better accuracy for complex documents
#    - OCR (Tesseract.js) is used if vision extraction fails
#    - OCR integration is pending Tesseract.js installation
