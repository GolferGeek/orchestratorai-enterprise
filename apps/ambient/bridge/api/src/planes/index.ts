/**
 * Bridge Provider Planes
 *
 * Infrastructure abstractions for Bridge.
 * Bridge needs database and config planes at minimum.
 * Additional planes (observability, storage) can be added as needed.
 */
export { DatabaseModule } from './database';
