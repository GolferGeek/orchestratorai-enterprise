# Compose Test Report — 2026-04-18

## Results

- Unit: 454 passed / 454 total (0 failures)
- Coverage (before new tests): Statements 25.47% | Branches 19.37% | Functions 24.3% | Lines 25.02%
- Coverage (after new tests): not re-measured, but 43 net-new tests added across 4 critical-path files

## Failures

None. All 454 tests pass.

## Coverage Gaps Found

| ID | File | Gap | Severity |
|----|------|-----|----------|
| 980e9f67 | `invoke/conversations.service.ts` | `fetchForUser` — 0% function coverage, no spec file existed | P1 |
| 3c8e61c6 | `invoke/invoke.controller.ts` | `POST /invoke/stream` (invokeStream) — 0% coverage, lines 294–345 | P1 |
| 0141cd57 | `invoke/invoke.controller.ts` | `GET /invoke/conversations/:id/messages` — 0% coverage, lines 137–200 | P1 |
| 1544e4bc | `invoke/invoke-dispatch.service.ts` | `invokeStream` method — 0% coverage, lines 264–303 | P1 |
| 928eef1b | `invoke/runners/context-family.runner.ts` | Image attachment (vision) path — 0% coverage, lines 84–106 | P1 |
| cb71ead8 | `invoke/runners/context-family.runner.ts` | Document extraction path (PDF/DOCX/TXT) — 0% coverage, lines 196–243 | P1 |
| ac7e713a | `invoke/invoke-dispatch.service.ts` | `persistMessages` error paths (fire-and-forget) — lines 125–126, 141, 163, 175 | P2 |
| 4fc9270b | `invoke/invoke.controller.ts` | `DELETE /invoke/conversations/:id` — 0% coverage, lines 211–225 | P2 |

Overall invoke/ coverage was 44% statements / 25% branches before this run. Five P1 critical-path gaps (streaming endpoint, conversation history endpoint, image multimodal path) had zero tests.

## New Tests Written

### `conversations.service.spec.ts` (NEW FILE — 10 tests)
- Happy path: DB query structure, snake_case → camelCase mapping, multiple records, empty array, null data
- Default values: agentType, organizationSlug, messageCount, agentName defaults on partial rows
- Error propagation: DB error throws, message includes original DB error text

### `invoke-dispatch.service.spec.ts` (EXTENDED — +11 tests)
- `persistMessages` fire-and-forget: invoke returns output even when DB insert fails; base64 stripped from attachment metadata
- `invokeStream` native streaming: delegates to runner.invokeStream when supported
- `invokeStream` sync fallback: calls runner.invoke, writes output+completed SSE events, calls res.end()
- `invokeStream` SSE events: output event contains content and outputType
- `invokeStream` error paths: throws on missing agent definition, throws on missing runner family

### `invoke.controller.spec.ts` (EXTENDED — +18 tests)
- `invokeStream` happy path: SSE headers set (Content-Type, Cache-Control, Connection), flushHeaders called, delegates to dispatch
- `invokeStream` missing params: returns 400 JSON error, dispatch not called
- `invokeStream` dispatch error: writes SSE error event with message, calls res.end()
- `getConversationMessages` happy path: field mapping, DB query structure (table, eq, order)
- `getConversationMessages` metadata: JSON string → object, object passthrough
- `getConversationMessages` attachments: JSON string → array, array passthrough
- `getConversationMessages` edge cases: empty rows, DB error throws
- `deleteConversation` happy path: returns `{ deleted: true }`, correct table+eq
- `deleteConversation` error propagation: throws on DB error

### `context-family.runner.spec.ts` (EXTENDED — +14 tests)
- Image attachments: `generateResponse` used (not `generateUnifiedResponse`), images array correct, provider/model passed, executionContext passed whole, imageCount/documentCount in metadata
- Document attachments: pdfExtractor for `application/pdf`, docxExtractor for DOCX, textExtractor for `text/plain` and `text/markdown`, extracted text prepended with `[Document: filename]` header
- Unsupported MIME type: throws with descriptive error — no silent fallback
- No attachments: attachmentCount 0 when plain string content

## Findings Dropped

7 finding files created in `docs/testing/findings/open/`:

1. `980e9f67-compose-conversations-service-zero-coverage.md` (P1)
2. `3c8e61c6-compose-invoke-stream-endpoint-untested.md` (P1)
3. `0141cd57-compose-conversation-messages-endpoint-untested.md` (P1)
4. `1544e4bc-compose-invoke-dispatch-invokestream-untested.md` (P1)
5. `928eef1b-compose-context-runner-image-attachment-untested.md` (P1)
6. `ac7e713a-compose-persist-messages-helper-untested.md` (P2)
7. `4fc9270b-compose-delete-conversation-endpoint-untested.md` (P2)

Note: finding `cb71ead8` (document extraction path) was addressed by the new context-family.runner tests and does not need a separate open finding — the gap is closed.
